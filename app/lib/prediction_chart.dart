import 'package:flutter/material.dart';

class CustomGraph extends StatelessWidget {
  const CustomGraph({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Graph will be here');
  }
}
